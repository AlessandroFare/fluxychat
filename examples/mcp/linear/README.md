# Linear MCP example (marketplace catalog `linear-mcp`)

Minimal stdio MCP server scanned by CI `mcp-audit`. Set `LINEAR_API_KEY` for live calls.

```bash
node server.mjs
```

Run [mcp-audit](https://github.com/adudley78/mcp-audit) in CI before publishing a fork to your marketplace.
