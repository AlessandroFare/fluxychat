# Notion MCP server (FluxyChat official example)

Search pages and append blocks for internal knowledge workflows.

```bash
cd examples/mcp/notion
npm install
export NOTION_TOKEN=secret_...
npm start
```

Catalog id: `notion-mcp`.
