# 2026-09-11

Open campaign: **full-surface tests**. Plan: `qa/full-surface-test-campaign.md`. Resume: integrated Playwright after JWT mint matches local D1 (`FLUXY_CONSOLE_API_KEY`, not a fake project id).

Done today: fasi 0–6 smoke + package public APIs (`sdk`/`react`/`protocol`/`ui`). Playwright no-JWT product gates green (9 passed); huddles two-tab skipped without SFU secrets. Protocol: `derived` inbound / `derived_set` outbound now match Room DO.

Also this week: Dependabot bumps (Next 16.3.4, Hono 4.13.7, xmldom 0.9.12, vitest 4.1.11, …). `image-size` still 2.0.2 with no JXL/HEIF patch on npm.
