# 2026-08-26

## Docs / llms.txt (agent catalog)

Closed the Fumadocs IA pass without rewriting the whole tree:

- New/used pages: Concepts, Broadcast, What to build
- Homepage + Core + Getting started cards (Liveblocks-shaped: concepts → primitives → gallery → llms.txt)
- Canonical `apps/docs/lib/llms-txt.ts`: recipes, JWT `POST /auth/token`, provider props, server_event names, UI kit, sitemap
- Fixed: comments digest lie, `use-chat` FluxyRealtimeProvider snippet, gallery examples on for-coding-agents
- `docs/llms.txt` header points at hosted catalog
- LB-DX-001/002/003 marked 🟩 in LIVEBLOCKS-PORTAL-COLLAB-DX-ROADMAP.md

Tell agents: read https://docs.fluxychat.com/llms.txt then build. Gold path still `npx @fluxy-chat/create-fluxy-chat@latest`.
