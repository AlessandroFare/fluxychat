# 2026-09-01

- Clerk public: `/status`, `/subprocessors`, `/features`, `/.well-known`.
- Get-started banner: pk_ first, console sign-in optional.
- Docs one source: pk_ + polls gallery + `iotAutoAgentId` on platform-status.
- Compare table: kernel rows only. Landing: no 14-adapter / OBS-as-product copy.
- CLI `0.5.18`: `--example polls`; hosted `setup:hosted` + `GET /public/demo-credentials`; `fluxy:deploy`.
- Worker: GET publishable-key, `/dev/provision` mints `pk_`, CLI bootstrap returns `publishableKey`.
- No npm publish this session. Hosted still needs `PUBLIC_DEMO_PUBLISHABLE_KEY` set on the live Worker to match Portal with zero console copy-paste.
