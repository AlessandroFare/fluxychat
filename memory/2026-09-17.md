# 2026-09-17

Integrated Playwright: 401 came from minting a JWT with `tid=dev-local` while local `FLUXY_CONSOLE_PROJECT_ID` is unset / D1 has another project. `e2e/admin-jwt.cjs` now probes `GET /rooms` and mints only via `/auth/token` after `/dev/provision`. Session stores `activeProject` from JWT `tid`. If provision is reused, need `FLUXY_CONSOLE_API_KEY` in `.dev.vars`.

JWT skip after `globalSetup`. Session seed: bind `/` with `waitUntil: commit`, no `/projects` reload (ERR_ABORTED when Next hung). Local run: `PLAYWRIGHT_SKIP_WEBSERVER=1` + `PLAYWRIGHT_CHANNEL=chrome`.

Kernel 5/5. Product 9/9 (projects, collab, fleet, iot, webhooks, notifications, embed, llm-keys, a2a). Huddles two-tab skipped (SFU in prod). Onboarding wizard skipped under Clerk hosted.
