# 2026-08-31

## Positioning

Wrote `docs/POSITIONING.md`. Room layer, not generic 30s realtime. Still ship a Portal-speed path so they do not own the funnel.

## 30s path (implemented)

`FluxyRealtimeProvider` accepts `publishableKey`. Client is ready on first render. SDK mints `POST /tokens/anonymous`. Guest JWT can join public rooms (no `roomId` claim). Quickstart A, hero, client-setup, publishable-keys, llms.txt, react README updated.

Packages: `@fluxy-chat/sdk` 0.6.7, `@fluxy-chat/react` 0.1.6 (peer `^0.6.7`), `@fluxy-chat/create-fluxy-chat` 0.5.16. Do not npm-publish until asked.

## Ops leftover

- Publish sdk 0.6.7 then react 0.1.6 then create-fluxy-chat 0.5.16.
- D1 `0221_project_publish_config.sql` on prod if not applied.
- Wrangler: `PUBLIC_GUEST_ENABLED` as secret vs `[vars]` conflict if still present.

## Room kernel DX (same day)

`useChat` + `useLiveCursors` share `sessionScope` `app` under the provider. Free plan copy is the weekend/hackathon plan (no card, pk_, cursors not metered). Docs: self-host cost, JWT refresh function, inbox gold path, transport `degraded-http`.

