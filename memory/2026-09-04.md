# 2026-09-04

Nested chat threads on existing `parentId`: `useThread` lens (shared room store under the provider), `GET /rooms/:id/threads` with opaque `nextCursor`, depth cap 8 (`ThreadDepthExceededError`), inbox `kind: thread`. Comment pins stay `useThreads` / `/comment-threads`.

Trust chrome: README pins 0.6.9 / 0.1.7, status page no fake incident, ui-kit guest docs include `publishableKey`, compare copy no longer lists spatial as kernel.

npm bump (not published from this session): `@fluxy-chat/sdk@0.6.9`, `@fluxy-chat/react@0.1.7` (peer `^0.6.9`). Publish sdk first, then react.

Marketing copy (2026-09-04): kernel hero, Bridges FAQ, encryption honesty, optional hero modules. Push dashboard+docs for live site.

Rooms console: `RoomThreadPane` uses `useThread` when you click "N replies" on a parent message.

Refactoring UI (tokens + primitives): defined `--shadow-1`…`--shadow-5` (and aliases for `--shadow-subtle*`, which were previously undefined). Header subtext is solid peach on orange. Cards/nav/banners/bubbles: one of border or shadow, not both. Console nav: inactive weight 400 + faded icons; active 600 + left accent.

Refactoring UI pass 2: `--brand`/`--ring` match CTA `#C2410C`; Tailwind radius 8px (16px only on 2xl/3xl); shadow-sm…xl mapped to the five elevations. Hero type pulled to 30/36/48 with weight on the accent, not 72px. Decorative `border+fill` cards (console, labs, agents, stream, kanban, Panel overrides) use shadow or a sunken fill. Inputs keep borders.
